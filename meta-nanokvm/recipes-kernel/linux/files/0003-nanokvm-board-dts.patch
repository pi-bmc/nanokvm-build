From: NanoKVM build <nbatkins@gmail.com>
Date: Tue, 8 Jul 2026 00:00:00 +0000
Subject: [PATCH] riscv: dts: sophgo: nanokvm board (usb, uart, gpio, ethernet)

NanoKVM board customisation, applied on top of the upstream USB backport
(0001-apply-dts-usb-dev.patch):

  * USB: the upstream node defaults nano-b to dr_mode = "host", but the
    NanoKVM BMC role needs the gadget stack (HID / mass-storage / ECM /
    UVC to the managed machine), so switch it to "peripheral".

  * The SDIO1 pads (SD1_D3 / SD1_D0) are deliberately left unmuxed. They
    used to carry a second i2c bus whose only child was an
    i2c-slave-eeprom presenting a 24c512 to the managed host. Nothing
    consumes that channel any more, so the bus, the slave EEPROM and their
    pinmux are all gone -- along with the DesignWare slave-mode probe they
    were the only reason for. The board's one remaining i2c bus is the
    LT6911C control bus enabled by patch 0012.

  * Ethernet: enable gmac0 + the mdio-mux so the SG2002 internal 10/100
    EPHY comes up as eth0.

Not upstream — local to this build.
---
diff --git a/arch/riscv/boot/dts/sophgo/sg2002-licheerv-nano-b.dts b/arch/riscv/boot/dts/sophgo/sg2002-licheerv-nano-b.dts
index b185377..d093ecb 100644
--- a/arch/riscv/boot/dts/sophgo/sg2002-licheerv-nano-b.dts
+++ b/arch/riscv/boot/dts/sophgo/sg2002-licheerv-nano-b.dts
@@ -95,6 +95,61 @@
 };
 
 &usb {
-	dr_mode = "host";
+	dr_mode = "peripheral";
+	status = "okay";
+};
+
+&pinctrl {
+	uart1_cfg: uart1-cfg {
+		uart1-pins {
+			pinmux = <PINMUX(PIN_JTAG_CPU_TMS, 6)>,
+				 <PINMUX(PIN_JTAG_CPU_TCK, 6)>;
+			bias-pull-up;
+			drive-strength-microamp = <10800>;
+			power-source = <3300>;
+		};
+	};
+
+	gpio0_cfg: gpio0-cfg {
+		gpio0-pins {
+			pinmux = <PINMUX(PIN_EMMC_CMD, 3)>,
+				 <PINMUX(PIN_EMMC_DAT1, 3)>,
+				 <PINMUX(PIN_EMMC_DAT0, 3)>,
+				 <PINMUX(PIN_EMMC_DAT3, 3)>;
+			bias-pull-up;
+			drive-strength-microamp = <10800>;
+			power-source = <3300>;
+		};
+	};
+};
+
+&gmac0 {
+	status = "okay";
+};
+
+&uart1 {
+	pinctrl-0 = <&uart1_cfg>;
+	pinctrl-names = "default";
+	status = "okay";
+};
+
+&gpio0 {
+	pinctrl-0 = <&gpio0_cfg>;
+	pinctrl-names = "default";
+};
+
+&porta {
+	gpio-line-names =
+		"", "", "", "", "", "", "", "",
+		"", "", "", "", "", "", "", "",
+		"", "", "", "", "", "", "",
+		"power-button",
+		"power-led",
+		"hdd-led",
+		"",
+		"reset-button";
+};
+
+&mdio {
 	status = "okay";
 };
